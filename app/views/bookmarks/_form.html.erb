  <%= simple_form_for (bookmark) do |f| %>
    <%= f.input :comment %>
    <%= f.association :list %>
    <%= f.association :movie %>
    <%= f.submit "Create Bookmark", class: "btn btn-light" %>
  <% end %>
